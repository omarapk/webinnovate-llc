; Upload limits for admin featured images (Cloudinary / local storage).
; Keep post_max_size >= upload_max_filesize.

upload_max_filesize=32M
post_max_size=32M

; Optional headroom for slow networks / remote storage.
max_execution_time=180
max_input_time=180
memory_limit=256M

